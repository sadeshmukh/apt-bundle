# Redis from PPA (Simpler example than PHP)
ppa ppa:redislabs/redis
apt redis-server

# Git from PPA (newer versions than Ubuntu repos)
ppa ppa:git-core/ppa
apt git

# Development tools from standard repos
apt build-essential
apt curl
apt vim

